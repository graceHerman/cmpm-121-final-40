{
  "name": "cmpm-121-final-40",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist",
    "build-deploy": "npm run build && npm run deploy"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0",
    "typescript": "~5.6.2",
    "vite": "^6.0.1"
  }
}
